<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>
  <b>Content:</b>
  <%= @post.content %>
</p>

<%= render 'comment_form' %>
<% @comments.each do |post| %>
  
    <p><%= comment.content %></p>
    <p><%= comment.name %></p>
    <p><%= comment.email %></p>
    <p><%= comment.website %></p>
  
<% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>